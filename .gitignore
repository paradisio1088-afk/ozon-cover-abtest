.env
__pycache__/
*.pyc
.venv/
venv/

# секреты — только локально, в облако идут через GitHub Secrets
data/secrets.json
data/app.run.log
data/launchd.out.log
data/launchd.err.log

# состояние теста (config, snapshots, blocks, results, журнал) коммитится —
# так облачный прогон продолжает с того же места
